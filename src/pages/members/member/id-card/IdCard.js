import React from "react";
import "./id-card.scss";

export const IdCard = React.forwardRef((props, ref) => {
  return (
    <div ref={ref} className="id-card">
      <div className="top">jhgkuy</div>
    </div>
  );
});
